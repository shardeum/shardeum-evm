package types

const (
	// EventTypeUpdateWhitelist defines the event type for updating the whitelist
	EventTypeUpdateWhitelist = "update_whitelist"
	
	// EventTypeAddValidator defines the event type for adding a validator
	EventTypeAddValidator = "add_validator"
	
	// EventTypeRemoveValidator defines the event type for removing a validator
	EventTypeRemoveValidator = "remove_validator"

	// AttributeKeyEnabled defines the attribute key for the enabled flag
	AttributeKeyEnabled = "enabled"
	
	// AttributeKeyAllowlistCount defines the attribute key for the allowlist count
	AttributeKeyAllowlistCount = "allowlist_count"
	
	// AttributeKeyValidator defines the attribute key for the validator address
	AttributeKeyValidator = "validator"
)
